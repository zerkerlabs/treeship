---
title: treeship verify
description: Verify a log entry or ship's log chain. Exit 0 = clean. Exit 1 = failed.
---

## Usage

```bash
treeship verify <artifact-id> [OPTIONS]
```

## What the verifier checks

1. Derives expected artifact ID from `sha256(PAE_bytes)[..16]`
2. Compares against stored ID -- tampering detected immediately
3. Verifies Ed25519 signature
4. Validates statement schema
5. Walks parent chain recursively
6. Enforces `action.approvalNonce == warrant.nonce` if present
7. Checks warrant expiry

## Examples

```bash
# Simple verify
treeship verify art_f7e6d5c4

# Machine-readable output for CI
treeship verify art_f7e6d5c4 --format json

# With trusted key (cross-org)
treeship verify art_passage_xyz --trusted-key ./company-a.pem

# Verify a bundle file
treeship verify mission.treeship
```

## JSON output

```bash
treeship verify art_f7e6d5c4 --format json
```

```json
{
  "outcome": "pass",
  "chain": 3,
  "approver": "human://rezo",
  "warrant_description": "approve stripe charge max $500",
  "checks": [
    { "name": "signature", "outcome": "pass" },
    { "name": "content_id", "outcome": "pass" },
    { "name": "schema", "outcome": "pass" },
    { "name": "nonce_binding", "outcome": "pass" },
    { "name": "warrant_expiry", "outcome": "pass" }
  ]
}
```

## Use in CI

```yaml
# .github/workflows/verify.yml
- name: Verify deployment chain
  run: |
    treeship verify $ARTIFACT_ID --format json | jq -e '.outcome == "pass"'
```
