- **CLI reference pages for hidden-but-shipped commands (W3-6 follow-up, DOC-9).** `hook`, `templates`/`template`, `prove`/`prove-chain`/`verify-proof`/`zk-setup`/`zk-tls-setup`, `approval`, and `declare` are implemented, tested, and shipped (several `stable` in `docs/feature-inventory.yml`) but had no reference page and are hidden from `--help` -- previously undocumented anywhere except a one-line mention on a concept page. New pages: `cli/hook.mdx`, `cli/template.mdx`, `cli/zk.mdx`, `cli/approval.mdx`, `cli/declare.mdx`, each opening with a Callout explaining what "hidden from `--help`" means here (implemented and stable, just not advertised) and, for the ZK commands, that release binaries ship without the `zk` feature -- `prove`/`prove-chain`/`verify-proof` exit 5 ("not compiled into this build") without it, while `zk-setup`/`zk-tls-setup` aren't feature-gated and run in every build. Flag tables were generated by hand-invoking `treeship <cmd> --help` on a local build and checked against the real CLI with `docs/scripts/check-cli-option-tables.py` (179 rows across 35 commands, 0 mismatches) rather than typed from memory. Linked the new pages from the relevant `feature-inventory.yml` entries' `docs:` lists and added all five to `cli/meta.json`'s sidebar.
