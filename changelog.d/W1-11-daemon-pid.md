- **`daemon status` no longer kills the daemon, and `daemon start` backgrounds.** The pid file holds `<pid> <start epoch>`, but the reader parsed the whole line as one number, so every read failed: `status` reported "stopped", deleted the file, and the daemon (which exits when its pid file disappears) died of a status check; `stop` then said "not running". The reader takes the pid, a pid file is removed only when it names a process that is gone, and a file that does not parse is reported and left alone. `daemon start` without `--foreground` now starts a detached child and returns with its pid; it used to run in the foreground and print "tip: run with &". (0.31.9 full test, CLI-8.) Two starts racing used to both truncate the pid file, so the loser wiped the winner's pid and left a daemon nobody could stop; the file is now locked before it is written, liveness is decided by the lock rather than by a pid that may have been reused, and a start succeeds only when its own child holds the lock. The daemon starts in its own session, so closing the terminal does not take it down.
