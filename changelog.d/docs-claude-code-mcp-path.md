- **Claude Code MCP-only docs no longer point at a file Claude Code never reads.** `docs/content/docs/integrations/claude-code.mdx`'s Method 3 and `integrations/claude-code/README.md`'s Method 2 both instructed a manual MCP config at `~/.claude/mcp.json` -- the same stale path CLI-16 (#494) fixed `treeship add claude-code` from writing to. Claude Code actually reads MCP servers from a project's `.mcp.json` or `~/.claude.json`'s top-level `mcpServers` key; both docs now say so and point the copy-paste template at `.mcp.json` in the project root.
