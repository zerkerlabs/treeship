- **Security claims registry; keystore/checkpoint/ZK wording rewritten (W3-1, DSEC-1/DSEC-2/DSEC-3/DOC-8).** Docs described the keystore as "machine-bound" and protected by a "passphrase" -- neither is true; it's an AES-256-GCM keystore under a key derived from a secret seed file stored beside it, no passphrase, and copying `.treeship/` is enough to sign elsewhere (`SECURITY.md`, `threat-model.md`, `security.mdx`). Checkpoints were described as externally anchored/witnessed; a hub checkpoint is single-signed by the publisher and the hub never countersigns -- only per-artifact Rekor anchoring (TS-2026-003) is a real external witness today, and `room-sessions.mdx`'s checkpoint-root/OpenTimestamps design is now marked as a design sketch, not built. `zero-knowledge.mdx` contradicted its own quarantine callout by saying the browser verifies Groth16 proofs "today"; `core-wasm/README.md` and `zk-circom/README.md` made the same overclaim and the latter referenced a nonexistent `setup/` ceremony directory. "Tamper-proof"/"immutable" language (`AGENTS.md`, `compliance.mdx`, a blog title) is now "tamper-evident," with the actual boundary stated (unsigned fields can change; the key holder can re-sign; an approver URI isn't proof of a specific human unless they sign with their own pinned key). Also fixed a wrong key-id derivation formula and a base32-vs-hex artifact-id typo in `threat-model.md`, and its stale `trust add --kind merlke-checkpoint` example / "no default trust root" claim (the built-in Sigstore `transparency_log` key is a default root).

  New: `docs/claims.yml` registers every claim above with a status (`shipped`/`next`/`designed`/`retired`) and a proof path, and `scripts/check-claims.py` (wired into `ci.yml`'s `docs-drift` job) fails if a banned phrase (passphrase, machine-bound, tamper-proof, immutable, anchored, witnessed, air-gapped) appears in the files `claims.yml` scans without a `<!-- claims:id -->` / `{/* claims:id */}` marker naming a real entry, or if a claim's `proof` path doesn't exist. Scoped to the specific files these findings named, not every doc in the repo -- the same English words are used correctly elsewhere for properties that have nothing to do with the keystore or the verifier (HTTP cache headers, a workflow spec's genuinely-immutable declared graph).

  `PLATFORM.md` is marked a historical snapshot at the top of the file rather than rewritten line by line; it is a frozen 0.4.0-era document (current release is far past it) and pointing readers at current docs is more honest than patching individual stale claims in a doc nobody should be reading as current.
