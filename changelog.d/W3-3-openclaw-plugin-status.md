- **`@treeship/openclaw-plugin`'s status now matches npm reality (W3-3, PKG-1).** The install matrix (`integrations/agents.json`, which generates `install.mdx` and the `.well-known/treeship-agents.json` mirror) marked the plugin `🟢 shipped` with `openclaw plugins install @treeship/openclaw-plugin` as the install command; that package was never published (`npm view` returns `E404`) and the command fails. Downgraded to `🟡 candidate` and swapped in the local build+install command that actually works today (`cd integrations/openclaw-plugin && npm install && npm run build && openclaw plugins install --local ./`); `integrations/openclaw-plugin/README.md` leads with that path now. Also fixed the neighboring Kimi Code plugin row, which advertised a `treeship integrations install kimi-code` command that doesn't exist. Regenerated both derived files from the JSON source rather than hand-editing them.
