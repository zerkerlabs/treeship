- **Unreleased judge flags labeled; hidden-command status made explicit (W3-6, DOC-5/DOC-9).** `cli/judge.mdx` documented `--contract`, `--state-out`, `--resolve`/`--by`/`--decision`/`--route`/`--reason` as shipped; they're implemented on `main` but not in the latest tagged CLI (0.31.9 rejects them). Added "(next release)" markers to each option-table row and a `Callout` at each section introducing them, pointing readers at `treeship --version` vs. the CHANGELOG rather than assuming the docs match their installed binary. The generated `command-matrix.mdx` (built from whatever this repo compiles, which can be ahead of the last tag) now says so in its own header, from `scripts/gen-cli-reference.py`'s template. Separately, `docs/feature-inventory.yml` marked `checkpoint`, `merkle *`, `harness *`, `approval *`, `bundle *`, `agent register` and `declare` `stable` with no indication they're `hide = true` in `main.rs` (not in `treeship --help`) -- added a `hidden: true` field (documented in the schema header) plus a clarifying note to each of the 6 affected entries, so "stable and hidden" reads as a deliberate, tracked design choice instead of an undocumented gap. Also added the missing `rekor-artifact-anchoring` inventory entry (shipped 0.31.9) and roadmap entries for `checkpoint-root-anchoring`, `rfc3161-timestamps` and `opentimestamps`. Per-command reference pages for the hidden surfaces that have none (`templates`, `prove`, `prove-chain`, `verify-proof`, `zk-setup`, `zk-tls-setup`, `hook`) are a separate, larger doc-writing task, not addressed here.
