- **`merkle verify` says "not pinned", not "signature invalid", for a checkpoint signer you have not pinned.** Following the CLI's own steps on a fresh ship (checkpoint, `merkle proof`, `merkle verify`) failed with "checkpoint signature invalid", exit 1, although the signature and the inclusion proof both held: the signer just was not pinned as `hub_checkpoint`. The core verifier already told the two apart (`Checkpoint::verify_detailed`); the CLI collapsed them. Now an unpinned signer exits **6**, a new code in the exit-code table, and names the key to confirm out of band before `treeship trust add <key_id> ed25519:<key> --kind hub_checkpoint` (no `--yes`: the key comes from the checkpoint itself). `--format json` gives `{"outcome": "not_pinned", "key_id", "public_key"}`. A signature or proof that does not hold still exits 1. Audit 2026-09-25, CLI-5.
- **`merkle publish` points at the attached hub's proof, not treeship.dev.** After publishing to a self-hosted hub, the hint still said `treeship.dev/merkle?id=…`, a page that knows nothing about that hub. It now prints `<hub>/v1/merkle/<artifact>` (the hub's own proof endpoint), and the treeship.dev page only for `api.treeship.dev`. Audit 2026-09-25, CLI-12 (the merkle half of W1-9).
