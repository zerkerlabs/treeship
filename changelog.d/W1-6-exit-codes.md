- **Every failure exits nonzero.** `prove`, `prove-chain` and `verify-proof` on a build without `--features zk` printed a warning and exited 0, so `treeship verify-proof /nope.zkproof` passed a script's exit-code check. They now exit 5, a new code for "not compiled into this build", and `otel` on a build without the feature does the same. `otel export` to an unreachable collector exited 0 after printing "✗ export failed"; `approval status` and `approval uses` on a grant nobody has heard of reported "unbounded, within max_uses" and exited 0; `resolve` on an agent with no card exited 0. All four are errors now (exit 1). A new contract test (`packages/cli/tests/contract.rs`) walks every command's `--help` and pins each of these failure paths to its exit code. (0.31.9 full test, CLI-6.)
