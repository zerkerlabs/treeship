- **Share URLs come from the hub you pushed to.** After `hub push` or `session report` to a self-hosted hub, the CLI printed `https://treeship.dev/verify/…` and `/receipt/…` addresses that held nothing, because the hub hard-coded them in its replies. The hub now builds them from configuration only: `TREESHIP_PUBLIC_URL` names the site that renders pages for its data; `TREESHIP_HUB_PUBLIC_URL` names the hub's own public origin, for a hub with no page site, which then answers with its own API URLs (`<hub>/v1/receipt/<id>`, `<hub>/v1/artifacts/<id>`), and `treeship verify <url>` accepts those; with neither set it keeps today's treeship.dev pages, so the public hub needs nothing. No request header is consulted, since these URLs are stored with the artifact and served to every later reader. The CLI prints what the hub returns, except that a hub which is not treeship's and still answers with a treeship.dev address is corrected to its own API URL, as is a hub that returns none; `session report` prints no preview or package page links for a hub that has no pages; `hub push` no longer suggests `treeship open`, a command that does not exist. The hub half takes effect on the public hub when it is next deployed. (0.31.9 full test, CLI-12.)
