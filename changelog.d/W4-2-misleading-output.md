- **Output no longer contradicts itself.** `verify-capability` and `resolve` opened with a green "✓" headline for a revoked card and said REVOKED two lines down; the headline is now the verdict. `vi check` printed `PASS` for every rule and then `FAIL` for the same mandate; rules read as passed only when the mandate is satisfied. `attest action --v2` signed an action outside its grant's scope without a word; it now warns at signing time (the receipt still records the violation, and `verify` still reports it). `grant revoke` on a revoked grant and `judge --resolve` on a resolved judgement minted a second receipt as if nothing had happened; both now refuse and name the standing one. `treeship ui` off a terminal said `Device not configured (os error 6)`; it now says it needs a terminal. `dashboard --host 0.0.0.0` said "local-only"; the mode line follows the bind address. `approve` said "the command will now proceed" when a shell hook cannot hold a command back; it now says to run it again if it did not run. Hints named commands that do not exist (`treeship zk-tls notary setup`, `treeship share last`); a test now checks every hint and template line against the binary's command tree. `trust add` and `keys list` showed different fingerprints for one key (one hashed the key's text, the other its bytes); both now hash the bytes. `verify-capability` on an artifact that is not a card says so instead of a JSON parse error. (0.31.9 full test, CLI-15 and P3-UX.)
