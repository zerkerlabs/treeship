- **Two-machine room-join docs (W1-3b follow-up).** `session join --out`, `session mint-challenge`, `session answer-challenge`, and `session countersign --pending` (#499) let a host and joiner finalize a room join across separate machines, gated on a mandatory liveness challenge -- none of this was documented. `docs/cli/session.mdx` gained full reference sections for `mint-challenge` and `answer-challenge`, and updated `join`/`countersign` entries covering `--out`, `--pending`, the challenge flags, the 10-minute `TREESHIP_JOIN_CHALLENGE_WINDOW_SECS` window, single-use enforcement, and the closed-session refusal. `concepts/multi-agent-sessions.mdx` gained a "Joining from another machine" walkthrough with the full flow and real CLI output, verified end to end against a locally built binary (two isolated `HOME`s standing in for two machines). `concepts/room-sessions.mdx`'s "What ships today" section now points at it instead of implying same-machine-only.
