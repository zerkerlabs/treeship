- **Regenerated the fictional command/SDK surface in agent skill files (W3-2).** `skills/treeship/SKILL.md`, `skills/treeship-perplexity/SKILL.md`, `integrations/hermes/treeship.skill/SKILL.md` and the whole Kimi Code bundle (`integrations/kimi-code-plugin/`) taught commands, SDK calls and Hub routes that don't exist: `treeship approve --approver`, `key show`, `treeship inspect`, `bundle verify`, `treeship attach <agent>`, a `Treeship`/`Ship.init()` class API, `dock_push`, `expires_in`, a `POST /v1/attest` endpoint, a websocket stream, and `TREESHIP_API_KEY`/`TREESHIP_AGENT`/`TREESHIP_HUB_ID`, none of which are real. Rewrote every command block and SDK example against the actual CLI (`packages/cli/src/main.rs`), the TS SDK's `ship()` factory (`packages/sdk-ts`), the Python SDK's scoped `attest_approval`/`hub_push` (`packages/sdk-python`), the real 9-tool MCP bridge (`bridges/mcp/src/server.ts`), and the real Hub routes (`packages/hub/main.go`). Fixed the three Kimi helper scripts' `dock_push` calls and its `config.toml.template`'s reference to a nonexistent `treeship integrations install` command. The oversized Kimi `references/{hub_api,mcp_bridge,typescript_sdk}.md` files, which had drifted the worst, are now short and point at the generated docs/source instead of re-deriving a route or command table that goes stale again.
