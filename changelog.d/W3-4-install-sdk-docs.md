- **Install and SDK docs re-verified against 0.31.9 (W3-4, DOC-6).** `guides/install.mdx` said Linux arm64 had no binary until v0.25 -- it's shipped in every release since v0.25.1, and both `curl | sh` and `npm install -g treeship` install it directly today, no cargo fallback. `core-wasm/README.md` named a nonexistent `treeship-core-wasm` npm package and showed a signature and an `init()` call that don't exist (correct: `@treeship/core-wasm`, `verify_envelope(envelopeJson, trustedKeysJson)` returning a JSON string, no init, two `wasm-pack` builds not one). `sdk/verify.mdx` and `sdk/typescript.mdx` carried a "core-wasm doesn't load under Node 22" warning that was fixed in v0.25.0 -- verified `@treeship/verify@0.31.9` loads and runs on Node 22.23.1 before rewriting it. `sdk/python.mdx` had two contradicting warnings saying `attest_approval()` and `wrap()` are "currently broken against CLI 0.24"; both work today (scoped approvals, `--format json` wrap) and the page already described the working behavior further down the same page. `cli/setup.mdx` said `setup --format json` returns `{}`; it returns a real structured payload (`SetupResult`) today. Also re-verified and dropped stale `v0.9.x`/`v0.24` version anchors in `sdk-ts/README.md`, `sdk-python/README.md`, and fixed a fictional `Ship.attestAction()` mention in `integrations/agent-skills.mdx`.
