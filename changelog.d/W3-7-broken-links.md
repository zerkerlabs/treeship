- **Broken doc links and anchors fixed (W3-7, P3-LINKS).** `about/changelog.mdx` had 5 repo-relative links that 404 as rendered docs pages -- three (`evaluator-kit`, `receipts-in-pull-requests`, `verification-reporting`) now point at their real `/guides/...` routes, and the two that live outside `docs/content/` (`verdict-invariant.md`, `TS-2026-002.md`) now link to their GitHub blob URLs. The `#the-gate` anchor was broken on 3 pages (`cli/judge.mdx`, `integrations/typesafe-jev.mdx`, `blog/any-judge-one-receipt.mdx`) -- the real heading is "The gate (PreToolUse)", slugified to `#the-gate-pretooluse`, which `cli/halt.mdx` and `reference/predicates.mdx` already used correctly. `blog/multi-agent-skills.mdx` linked to a stale SDK anchor computed from an old `attest_approval` signature; recomputed against the current one. `integrations/meta.json` was missing `buzz`, `grok-bot` and `rig` from its sidebar `pages` list even though all three `.mdx` files exist -- they were reachable only by typing the URL directly, now added to the sidebar.
