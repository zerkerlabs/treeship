- **The hub sends HSTS and nosniff.** Every response from the hub carries `X-Content-Type-Options: nosniff`, and every response that arrived over TLS (directly, or through a proxy that says so with `X-Forwarded-Proto`) carries `Strict-Transport-Security: max-age=63072000; includeSubDomains`; plain-HTTP local development is untouched. Takes effect on api.treeship.dev when it is next deployed. (0.31.9 full test, WEB-3, hub half.)
